export interface IVehicleData {
  licensePlate: string;
  owner: string;
  carModel: string;
  companyId: string;
}

export interface IVehicle {
  id: string;
  licensePlate: string;
  owner: string;
  carModel: string;
  companyId: string;
  companyName: string;
}

export interface IVehicleSelect {
  id: string;
  licensePlate: string;
  owner: string;
}
  
